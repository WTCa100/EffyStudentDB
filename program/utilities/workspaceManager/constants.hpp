#pragma once

#include <string>

const std::string directoryDatabase("database");

const std::string directorySchemas("schemas");
const std::string directorySchemasSubdir(directoryDatabase);

const std::string directoryLogs("logs");

const std::string fileBase("base.sql");
const std::string fileBaseSubdir("database/schemas");

const std::string fileDatabase("Effy.db");
const std::string fileDatabaseSubdir(directoryDatabase);